# Friendly ePub Reader

This reader allows people to upload their epubs.

## Usage

## Testing
- The epub files are handled by (Git LFS)[https://git-lfs.github.com/], install it to get access to the epub files for testing.
